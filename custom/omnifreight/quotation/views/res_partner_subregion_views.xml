<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_partner_form_inherit_import_export" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.import.export</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="arch" type="xml">
            <xpath expr="//page[@name='contact_addresses']" position="after">
                <page string="Trade Regions" name="trade_regions" invisible="is_employee">

                    <!-- Section: UN Country and Subregion -->
                    <group>
                        <group string="Contact Region">
                            <!-- Field editable only by Admins -->
                            <field name="un_country_id" string="Country"  />
                            <!-- Always readonly -->
                            <field name="un_subregion_id" string="Subregion" />
                        </group>
                    </group>

                    <!-- Section: Trade Areas -->
                    <group>
                        <group string="Import Information">
                            <!-- Field editable only by Admins -->
                            <field name="buy_country_ids" widget="many2many_tags"
                                string="Importing From" invisible="is_linked_to_company" />

                            <field name="expert_buy_country_ids" string="Importing From"
                                widget="many2many_tags" invisible="not is_linked_to_company" options='{"no_open": True, "no_create": True}' />
                            <field name="buy_region_ids" string="Import Region"
                                widget="many2many_tags" invisible="is_linked_to_company" />
                            <field name="expert_buy_region_display" string="Import Region"
                                widget="many2many_tags" invisible="not is_linked_to_company" />
                        </group>
                        <group string="Export Information">
                            <!-- Field editable only by Admins -->
                            <field name="ship_country_ids" widget="many2many_tags"
                                string="Exporting To" invisible="is_linked_to_company" />
                            <field name="expert_ship_country_ids" string="Exporting To"
                                widget="many2many_tags" invisible="not is_linked_to_company" options='{"no_open": True, "no_create": True}' />

                            <field name="ship_region_ids" string="Export Region"
                                widget="many2many_tags" invisible="is_linked_to_company" />
                            <field name="expert_ship_region_display" string="Export Region"
                                widget="many2many_tags" invisible="not is_linked_to_company" />
                        </group>
                    </group>
                </page>
                <page string="Contacts &amp; Addresses" name="contact_addresses"
                    autofocus="autofocus" invisible="not is_company">
                    <field name="contact_ids" mode="kanban"
                        context="{'default_parent_id': id, 'default_street': street, 'default_street2': street2, 'default_city': city, 'default_state_id': state_id, 'default_zip': zip, 'default_country_id': country_id, 'default_lang': lang, 'default_user_id': user_id, 'default_type': 'other'}" readonly="True" >
                        <kanban>
                            <field name="name" />
                            <field name="email" />
                            <field name="phone" />
                            <field name="role_ids" />
                            <field name="avatar_128" />
                            <templates>
                                <t t-name="card" class="flex-row">
                                    <aside class="o_kanban_aside_full">
                                        <field name="avatar_128" class="o_kanban_image_fill w-100"
                                            widget="image"
                                            options="{'img_class': 'object-fit-cover'}"
                                            alt="Contact Image" />
                                    </aside>
                                    <main class="ps-2 ps-md-0">
                                        <field name="name" />
                                        <field name="role_ids" widget="many2many_tags"
                                            options="{'color_field': 'color'}" />
                                        <field name="email" widget="email" />
                                        <div t-if="record.type.raw_value != 'contact'">
                                            <div>
                                                <field class="me-1" name="zip" />
                                                <field name="city" />
                                            </div>
                                            <field class="me-1" name="state_id" />
                                            <field name="country_id" />
                                        </div>
                                        <div t-if="record.phone.raw_value">Phone: <field
                                                name="phone" /></div>
                                        <!-- <div t-if="record.mobile.raw_value">Mobile: <field
                                                name="mobile" /></div> -->
                                    </main>
                                </t>
                            </templates>
                        </kanban>
                        <form string="Contact / Address">
                            <sheet>
                                <field name="types" required="1" widget="radio"
                                    options="{'horizontal': true}" />
                                <div class="text-muted oe_edit_only">
                                    <p class="mb-0" invisible="types != 'contact'">
                                        <span>Use this to organize the contact details of employees
                                            of a given company
                                            (e.g. CEO, CFO, ...).</span>
                                    </p>
                                    <p class="mb-0" invisible="types != 'invoice'">
                                        <span>Preferred address for all invoices. Selected by
                                            default when you invoice an
                                            order that belongs to this company.</span>
                                    </p>
                                    <p class="mb-0" invisible="types != 'delivery'">
                                        <span>Preferred address for all deliveries. Selected by
                                            default when you deliver
                                            an order that belongs to this company.</span>
                                    </p>
                                    <p class="mb-0" invisible="types != 'loading'">
                                        <span>Preferred address for loading.</span>
                                    </p>
                                    <p class="mb-0" invisible="types != 'other'">
                                        <span>Other address for the company (e.g. subsidiary, ...)</span>
                                    </p>
                                </div>
                                <hr />
                                <group>
                                    <group>
                                        <field name="name" string="Contact Name"
                                            required="type == 'contact'"
                                            placeholder="e.g. New Address" />
                                        <field name="role_ids" placeholder="e.g. Sales Director"
                                            invisible="types != 'contact'" widget="many2many_tags"
                                            options="{'color_field': 'color'}"
                                            domain="[('id', 'in', available_roles)]" />
                                        <label for="street" string="Address"
                                            invisible="type == 'contact'" />
                                        <div>
                                            <group>
                                                <field name="country_id" string="Country" placeholder="Country"
                                                     options='{"no_open": True, "no_create": True}' />
                                                <field name="city" string="City" placeholder="City" />
                                                <field name="zip" string="ZIP Code" placeholder="ZIP Code."/>                                                    
                                                <field name="line3" string="P.O. BOX" placeholder="P.O. BOX" />
                                                <field name="state_id" string="State" placeholder="State"
                                                    options="{'no_open': True, 'no_quick_create': True}"
                                                    context="{'country_id': country_id, 'default_country_id': country_id, 'zip': zip}" />
                                                <field name="street" string="Street" placeholder="Street ..."/>
                                                <field name="street2" string="Street 2" placeholder="Street 2..." />  
                                            </group>
                                        </div>
                                    </group>
                                    <group>
                                        <field name="email" widget="email" />
                                        <field name="phone" widget="phone" />
                                        <!-- <field name="mobile" widget="phone" string="Mobile(WhatsApp)"/> -->
                                        <field name="omnifreight_individual_religion" />
                                    </group>
                                </group>
                                <group>
                                    <field name="comment" placeholder="Internal notes..."
                                        nolabel="1" />
                                </group>
                                <field name="lang" invisible="True" />
                            </sheet>
                        </form>
                    </field>
                </page>
                <page name='internal_notes' string="Internal Notes">
                    <field name="comment" placeholder="Internal notes..." />
                </page>
                <page name='sales_purchases' string="Sales &amp; Purchase"
                    invisible="company_category == 'supplier' or is_employee">
                    <group name="container_row_2">
                        <group string="Sales" name="sale" priority="1">
                            <field name="user_id" widget="many2one_avatar_user"
                                domain="[('share', '=', False)]" />
                        </group>
                        <group string="Purchase" name="purchase" priority="2">
                        </group>
                        <group name="misc" string="Misc">
                            <field name="company_registry" invisible="parent_id" />
                            <field name="ref" string="Reference" />
                            <field name="company_id" groups="base.group_multi_company"
                                options="{'no_create': True}" readonly="parent_id" force_save="1" />
                            <field name="company_id" invisible="1" /> <!-- need to be able to to
                            compute based on
              the company even without multi company groups -->
                            <field name="industry_id" invisible="not is_company"
                                options="{'no_create': True}" />

                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>